apiVersion: v1
kind: PersistentVolume
metadata:
  name: openssh-server-pv
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 100Mi
  accessModes:
  - ReadWriteOnce
  hostPath:
    path: "/ssh/config"